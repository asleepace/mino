// Test Mino compile
const someString = "Hello world!"
const view = @html {
  <div class="greet">${someString}</div>
}

const styles = @css {
  .greet { color: red; }
}

function greet(name) {
  return view(`${name}`);
}

export { view, styles, greet };

